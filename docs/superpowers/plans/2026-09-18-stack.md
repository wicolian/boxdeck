# Boxdeck stack freshness implementation plan

> **For agentic workers:** Execute this plan task by task with fresh verification at each checkpoint.

**Goal:** Bring the Boxdeck toolchain and CI pins to the current vendor releases verified on 2026-09-18, document compatibility, and add automated drift checks.

**Architecture:** Keep runtime behavior unchanged. Update Go module toolchain declarations, GitHub Action majors, Swift package language settings, and packaging metadata. Add a small source-driven shell check and a Dependabot configuration so future updates are visible without changing product code.

**Tech Stack:** Go 1.27.1, GitHub Actions, Swift Package Manager, XcodeGen, Homebrew formula and cask Ruby, Dependabot YAML, POSIX shell, Python YAML validation.

**Spec:** `/home/ubuntu/codes/boxdeck/.work/phase5/PROMPT-stack.md`

## Global Constraints

- Do not touch `android/`, `.github/workflows/android.yml`, `mcp*.go`, or MCP sections of `index.html`.
- Use live vendor release pages and official documentation, not remembered versions.
- Keep changes to version pins, CI maintenance, packaging syntax, compatibility documentation, and required reports.
- Never push or merge `main`; push only `phase5-stack` and open a PR against `main`.
- Do not include the banned middle dot or dash punctuation characters in changed artifacts.

### Task 1: Record the verified stack baseline

**Files:**
- Create: `docs/stack.md`
- Modify: `README.md`, `API.md`

- [ ] Capture the exact current vendor versions, dates, and URLs for Go, Actions, systray, XcodeGen, Swift, Apple SDK support, runner mappings, Dependabot, and Homebrew syntax.
- [ ] Write the required table with component, pinned before, latest found, source URL, and bumped to columns.
- [ ] Replace stale Go and platform version prose with one link to `docs/stack.md`.

### Task 2: Bump build and workflow pins

**Files:**
- Modify: `go.mod`, `cmd/boxdeck-bar/go.mod`, `Package.swift`, `apple/BoxdeckKit/Package.swift`, `apple/project.yml`
- Modify: `.github/workflows/ios.yml`, `.github/workflows/macos-bar.yml`, `.github/workflows/release.yml`
- Create: `.github/workflows/stack-check.yml`
- Create: `scripts/stack-check.sh`

- [ ] Set both Go modules to Go 1.27.1 so `go-version-file` selects the verified toolchain.
- [ ] Move checkout, setup-go, upload-artifact, and download-artifact to their verified latest major tags and annotate each exact release and date.
- [ ] Add Xcode and runner version evidence steps without selecting a preview toolchain, because `macos-latest` currently supplies macOS 26 arm64 and default Xcode 26.6.
- [ ] Move Swift manifests to Swift tools 6.3 and XcodeGen project language settings to Swift 6.
- [ ] Add `workflow_dispatch` to release builds and skip only the publish job for manual runs.
- [ ] Add a weekly and manual stack-check workflow using the under-80-line script.

### Task 3: Keep packaging and dependency automation current

**Files:**
- Modify: `scripts/tap/Formula/boxdeck.rb`, `scripts/tap/Casks/boxdeck-bar.rb`, `scripts/tap/update.sh`
- Create: `.github/dependabot.yml`

- [ ] Add current Homebrew `livecheck` blocks while retaining valid `service do`, `on_macos`, and `on_linux` forms.
- [ ] Remove the stale hard-coded release placeholder from the tap renderer and keep generated release versions driven by its argument.
- [ ] Configure weekly GitHub Actions, Go modules, Gradle Android, and Swift updates, grouping minor and patch updates.
- [ ] Use a valid root Swift directory for the macOS root `Package.swift`, and document that it targets `macos/BoxdeckBar`.

### Task 4: Verify, deliver, and leave evidence

**Files:**
- Create: `/home/ubuntu/codes/boxdeck/.work/phase5/reports/stack.md`
- Create: `/home/ubuntu/codes/boxdeck/.work/phase5/reports/stack.done`

- [ ] Run Go vet, tests, and all requested cross-builds in both modules.
- [ ] Validate every touched workflow with `python3 -c 'import yaml'`.
- [ ] Run the release, iOS, macOS bar, and stack-check workflows on `phase5-stack`, recording URLs and honest failures or blocks.
- [ ] Fetch and rebase `origin/main`, rerun local gates, inspect the final diff, and sweep changed files for banned punctuation.
- [ ] Commit in small scoped commits, push `phase5-stack`, open the PR against `main` with the required no-visible-change before/after block, and restore GitHub auth.
- [ ] Write the report completely, verify it, then create and verify the literal `.done` marker last.
